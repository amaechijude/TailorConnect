from django.urls import path
from . import views

urlpatterns = [
    # path('register', views.index, name='api_registerr'),
    # path('login_user', views.cartView, name='api_login_user'),
    # path('logout_user', views.contact, name='api_logout_user'),
]
